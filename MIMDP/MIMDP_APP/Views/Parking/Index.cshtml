@model MIMDP_APP.Models.Parking.ViewModels.ParkingInfoViewModel;
@{
}
<style>
    .index {
        display: flex;
    }

    .contenido {
        width: 100%;
        padding: 20px;
    }

    .container-custom {
        width: 100%;
        height: auto;
        border: 1px solid #ccc;
        padding: 20px;
        margin-bottom: 20px;
    }
</style>

<partial name="_Header" model='TempData["UserName"]'></partial>

<div class="index">
        <partial name="_Sidebar" />

    <div class="contenido">
        <div class="container-custom">
            <form asp-action="SubmitForm" method="post">
                <div class="form-group">
                    <label asp-for="NewParkingRequest.Patente">Número de la Patente</label>
                    <select asp-for="NewParkingRequest.Patente" class="form-control" asp-items="@(new SelectList(Model.ParkingInfoResponse.Autos, "Patente", "Patente"))">
                        <option value="">Seleccione una de sus patentes</option>
                    </select>
                    <span asp-validation-for="NewParkingRequest.Patente" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="NewParkingRequest.Duracion">Duracion (En Horas)</label>
                    <input asp-for="NewParkingRequest.Duracion" type="number" class="form-control" required>
                    <span asp-validation-for="NewParkingRequest.Duracion" class="text-danger"></span>

                </div>
                <div class="form-group">
                    <label asp-for="NewParkingRequest.Direccion">Dirección</label>
                    <input asp-for="NewParkingRequest.Direccion" type="text" class="form-control" required>
                    <span asp-validation-for="NewParkingRequest.Direccion" class="text-danger"></span>

                </div>
                <button asp-action="NewParking" asp-controller="Parking" type="submit" class="btn btn-primary">Submit</button>
            </form>
        </div>

        <table class="table table-dark" style="width:100%">
            <thead>
                <tr>
                    <th scope="col">Id</th>
                    <th scope="col">Patente</th>
                    <th scope="col">Direccion</th>
                    <th scope="col">Duración</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var parking in Model.ParkingInfoResponse.Parking)
                {
                    <tr>
                        <th scope="row">@parking.Id</th>
                        <td>@parking.Patente</td>
                        <td>@parking.Direccion</td>
                        <td>@parking.DuracionEnHoras</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

<partial name="_Footer"></partial>